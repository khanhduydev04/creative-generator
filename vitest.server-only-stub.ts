// Test stub for the `server-only` package. It only exists to throw at build
// time when a server module is pulled into a client bundle; in unit tests we
// import server modules directly, so this is intentionally a no-op.
export {};
